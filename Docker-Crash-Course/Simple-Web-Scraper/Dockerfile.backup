FROM python:3.9-slim

WORKDIR /app

# Step 1: Copy Requirements (Layer Caching!)
COPY requirements.txt .

# Step 2: Install Dependencies (The Key Lesson)
RUN pip install --no-cache-dir -r requirements.txt

# Step 3: Copy Code
COPY . .

CMD ["python", "scraper_get_title.py"]